# Discord Pokemon Bot

## Setup

1. Activate the virtual environment.
2. Install dependencies:
   `pip install -r requirements.txt`
3. Copy `.env.example` to `.env`.
4. Put your Discord bot token into `.env`.
5. Run:
   `python bot.py`

## Test command

Send `!ping` in a server where the bot is present.
